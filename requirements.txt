# AirDrawVocab Unified AI
# Khuyến nghị: Python 3.11 hoặc 3.12 64-bit. Không khuyến nghị Python 3.13 vì TensorFlow/MediaPipe dễ lỗi.

# Core ML
tensorflow>=2.15,<2.22
numpy>=1.26,<2.3
scikit-learn>=1.3,<2.0

# Computer vision + face recognition
# Cần opencv-contrib-python để có cv2.face.LBPHFaceRecognizer_create
opencv-contrib-python>=4.10,<5.0
mediapipe>=0.10.30,<0.11
pillow>=10.0

# Desktop game
pygame>=2.6,<3.0
pyttsx3>=2.90

# Backend chatbot API
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart>=0.0.9
requests>=2.31.0
python-dotenv>=1.0.0
httpx>=0.27.0

# Training/report
matplotlib>=3.8,<4.0
seaborn>=0.13,<0.14

# Experiment tracking (Phase 1 - tùy chọn). Nếu không cài, các script train
# vẫn chạy bình thường (mlflow_utils.py tự động bỏ qua tracking).
mlflow>=2.12.0,<3.0

# Windows runtime helper
msvc-runtime>=14.40; platform_system == "Windows"
